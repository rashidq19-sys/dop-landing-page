# SEO Weekly Check — 2026-06-01

## TL;DR

First weekly check — all 10 prerendered pages green (titles, canonicals, no noindex); one new blog post (`/blog/amazon-cortex-dcr-score`) needs a GSC indexing request; live HTTP status unverifiable from cloud container IPs (see note below).

---

## Auto-detected site health

| Check | Status | Notes |
|---|---|---|
| 10 URL probes — prerendered HTML | ✅ | Verified from `dist/public/`; see caveat on live HTTP status below |
| Live HTTP status (all 10 URLs) | ⚠️ | Returns 403 from cloud container IPs — Cloudflare/Railway WAF blocking datacenter ranges. **Not a site outage** — real users load fine. Probe manually in browser to confirm. |
| Sitemap | ✅ | 11 `<loc>` entries (10 core pages + new DCR blog post added 2026-05-29) |
| robots.txt | ✅ | Allows `/`; disallows `/admin`, `/download`, `/api/`; Sitemap line present |
| 404 handling | ✅ (code only) | `PUBLIC_ROUTE_PATTERNS` allowlist verified in `server/index.ts`; live 404 response unverifiable due to IP block |
| 301 redirects | ✅ (code only) | `/features/rota` → `/dsp-rota-management`, `/features/payroll` → `/dsp-invoicing-payroll`, `/features/scorecard` → `/driver-performance-tracking`; plus `/$` and `/%24` → `/` (latest commit) |

### URL probe detail (prerendered HTML from `dist/public/`)

| URL | Expected title contains | Actual title | Canonical | noindex? |
|---|---|---|---|---|
| `/` | DSPOps — Amazon DSP Management Platform | DSPOps — Amazon DSP Management Platform for UK Delivery Service Partners | `https://dspops.app/` | absent ✅ |
| `/amazon-dsp-management-software` | Amazon DSP Management Software UK | Amazon DSP Management Software UK \| DSPOps | correct ✅ | absent ✅ |
| `/driver-performance-tracking` | Driver Performance Tracking | DSP Driver Performance Tracking Software \| DSPOps | correct ✅ | absent ✅ |
| `/van-inspection-app` | Van Inspection App | Van Inspection App for Amazon DSP Fleets \| DSPOps | correct ✅ | absent ✅ |
| `/dsp-rota-management` | Rota Management | DSP Rota Management Software \| DSPOps | correct ✅ | absent ✅ |
| `/dsp-invoicing-payroll` | Invoicing and Payroll | DSP Invoicing and Payroll Support Software \| DSPOps | correct ✅ | absent ✅ |
| `/dsp-compliance-tools` | Compliance Tools | Amazon DSP Compliance Tools \| DSPOps | correct ✅ | absent ✅ |
| `/privacy` | Privacy Policy | Privacy Policy \| DSPOps | correct ✅ | absent ✅ |
| `/blog` | Blog | DSPOps Blog — Practical guides for UK Amazon DSPs | correct ✅ | absent ✅ |
| `/blog/improve-amazon-cortex-scorecard` | Cortex scorecard | How to improve your Amazon Cortex scorecard \| DSPOps | correct ✅ | absent ✅ |

**Bonus — new URL not in original probe list:**

| URL | Title | Canonical | noindex? |
|---|---|---|---|
| `/blog/amazon-cortex-dcr-score` | Amazon Cortex DCR Score: What's Good and How to Improve It \| DSPOps | `https://dspops.app/blog/amazon-cortex-dcr-score` ✅ | absent ✅ |

This URL is in `sitemap.xml` ✅, in `scripts/prerender.mjs` ROUTES ✅, and in `PUBLIC_ROUTE_PATTERNS` via the `/^\/blog\/[a-z0-9-]+$/` regex ✅. **Needs a GSC indexing request** (see action items).

### Note on live HTTP probes

The remote execution environment runs on a cloud datacenter IP range. Both `curl` and `WebFetch` returned `403 Forbidden` for all 10 URLs. This is standard behaviour from Cloudflare or Railway's bot protection — it blocks automated requests from known cloud/datacenter IP ranges while passing real user browsers. It does **not** indicate site downtime. All live probes in future reports will share this limitation; manual browser checks are the workaround.

---

## SEO-relevant changes this week

This is the first weekly check, so all git history is covered below. Listing only SEO-impacting commits:

| Commit | Message | SEO implication |
|---|---|---|
| `d972260` | fix(seo): 301-redirect /$ and /%24 to / | Eliminates legacy garbage URLs (`/$`, `/%24`) that were reported as soft-404s in GSC. Google should deindex them quickly once it crawls the redirects. |
| `b449a7e` | feat(blog): add DCR score post; support separate H1 and meta title per post | New indexable URL `/blog/amazon-cortex-dcr-score`. The `metaTitle` field decouples the `<title>` tag from the H1 — good for CTR tuning without changing content. Needs GSC indexing request. |
| `3ef312a` | fix(seo): eliminate duplicate FAQPage JSON-LD on feature pages; fix viewport zoom | Duplicate `FAQPage` schema blocks were causing rich-result errors. Fixing this may restore FAQ rich snippets on feature pages. Fix for viewport zoom removes a mobile usability issue. |
| `60fa97f` | build(deploy): ship prerendered dist/public from git, drop Puppeteer from Railway | Major architecture change — prerendered HTML is now committed to git rather than generated at deploy time. Eliminates Puppeteer/Chromium install failures on Railway. Trade-off: HTML must be regenerated and committed locally after content changes. |
| `e70ed06` | fix(seo): differentiate home vs pillar title, rebrand 404, add GSC checklist | Homepage and pillar page titles are now distinct — reduces risk of "Duplicate without user-selected canonical" in GSC coverage. |
| `e7283b3` | feat(seo): keyword-targeted pages, prerendering, technical SEO overhaul | Full SEO foundation: 7 keyword-targeted pages, per-page canonicals, prerendering, real 404s, 301 redirects from `/features/*`. This is the commit that made the site indexable properly. |

---

## What to do in Google Search Console this morning (10 min)

Open https://search.google.com/search-console → dspops.app property.

1. **Pages** report (left sidebar → "Pages"):
   - Total indexed: _____ (compare vs last week — this is week 1, so record baseline)
   - Total not indexed: _____
   Under "Why pages aren't indexed":
   - Crawled — currently not indexed: _____ (target: trending down)
   - Discovered — currently not indexed: _____ (target: trending down)
   - Soft 404: _____ (target: 0 — recent fix for `/$` / `/%24` should reduce this)
   - Duplicate without user-selected canonical: _____ (target: 0 — recent title differentiation should help)
   - Blocked by robots.txt: _____ (only `/admin` and `/download` expected)

2. **URL Inspection re-checks — ACTION REQUIRED:**

   The new DCR score blog post was added 2026-05-29 and has not yet had an indexing request (based on commit history). Inspect and request indexing:
   - `https://dspops.app/blog/amazon-cortex-dcr-score` → paste into inspector → **Request Indexing**

   Also check whether the `/blog/improve-amazon-cortex-scorecard` post (added 2026-05-18) has been indexed. If it shows "Crawled — currently not indexed" or "Discovered — currently not indexed", request indexing again.

   For any other URL under "Discovered — currently not indexed" or "Crawled — currently not indexed" that's been there >10 days:
   - Paste into inspector → click **Request Indexing** → confirm

3. **Performance** report (left sidebar → "Performance"):
   - Impressions this week: _____ (vs last week)
   - Clicks this week: _____
   - Average CTR: _____
   - Top 5 queries: _____
   - Top 5 pages: _____
   - Flag any page with >50 impressions and <2% CTR — title/description rewrite candidate

4. **Sitemaps** (left sidebar → "Sitemaps"):
   - Was `https://dspops.app/sitemap.xml` re-submitted after the DCR blog post was added? The sitemap now has 11 URLs. If the last-submitted version had only 10, re-submit: delete the old entry and re-add `sitemap.xml`.
   - Last read date: _____ — should be recent
   - Status: should be Success

5. **Core Web Vitals** (left sidebar → "Core Web Vitals"):
   - Good URLs: _____
   - Need improvement: _____
   - Poor: _____
   - Note: the initial bundle is ~165 KB gzipped with code-splitting. CWV should be healthy, but verify once Google has collected enough field data.

6. **Enhancements** (left sidebar — FAQs / Breadcrumbs / SoftwareApplication):
   - Valid: _____ / total
   - Commit `3ef312a` fixed duplicate FAQPage JSON-LD on feature pages — check whether the errors that were previously present have cleared.
   - If new errors appear on any page, use https://search.google.com/test/rich-results to diagnose.

---

## Blog topic suggestion for this week

**Existing posts:** "How to improve your Amazon Cortex scorecard" (2026-05-18), "What is a good Amazon Cortex DCR score" (2026-05-29). Both target Cortex metrics. Branching into compliance content will build topical authority on a different cluster and support the `/dsp-compliance-tools` feature page.

**This week's suggestion:**

- **Title:** Right to Work checks for Amazon DSP drivers: the complete UK guide
- **Why it'd rank:** "right to work check DSP" and "right to work Amazon driver UK" are practical, intent-driven searches with low competition — DSP operators are legally required to do these and most have no systematic process. The post provides direct internal linking support for the `/dsp-compliance-tools` feature page.
- **Outline:**
  - What is a Right to Work check and why DSPs are liable (not just Amazon)
  - Which documents count as acceptable evidence (List A vs List B)
  - How often to re-check drivers on time-limited visas
  - The cost of getting it wrong: Home Office civil penalties up to £20,000 per illegal worker
  - Manual vs digital RTW checks — what the current Home Office guidance says
  - How DSPOps tracks RTW expiry dates and flags upcoming renewals automatically
- **Suggested slug:** `right-to-work-checks-dsp-drivers-uk`
- **Internal links:** `/dsp-compliance-tools` (multiple), `/dsp-rota-management` (mention scheduling around expired docs)

---

## Action items for this week (max 5, prioritised)

1. **Request GSC indexing for `/blog/amazon-cortex-dcr-score`** — new post added 2026-05-29, prerendered and in sitemap, but has not been submitted to GSC. Do this today.
2. **Re-submit sitemap in GSC** — the sitemap grew from 10 to 11 URLs with the DCR post. Delete the old sitemap entry in GSC and re-add `sitemap.xml` to force a fresh read.
3. **Verify `/$` and `/%24` are no longer appearing as soft-404 in GSC** — commit `d972260` added 301 redirects for these garbage URLs. Check the GSC Pages report to confirm the soft-404 count is trending to 0.
4. **Check FAQPage rich results after the duplicate JSON-LD fix** — commit `3ef312a` removed duplicate schema blocks. Use https://search.google.com/test/rich-results on `/driver-performance-tracking`, `/dsp-rota-management`, and `/dsp-compliance-tools` to confirm FAQs now validate cleanly.
5. **Write and publish "Right to Work checks for DSP drivers"** — supports the compliance tools page, targets a distinct keyword cluster from the existing Cortex posts, and is evergreen content.

---

## Backlink reminder

New-domain SEO is bottlenecked by inbound links. Target: 10–30 quality backlinks in the first 3 months.

**This week's suggested outreach target:**

- **Where:** G2 (g2.com) — SaaS product review and discovery directory
- **What:** Create a free vendor profile for DSPOps. Add the product description, category (Fleet Management / Field Service / Last-Mile Delivery Software), logo, screenshots, and website URL. Request reviews from any early users or beta testers.
- **Why it works:** G2 has very high domain authority (DA 91). Even a listing with zero reviews passes link equity and appears in Google searches for "[product name] reviews" and "[category] software". It's a one-time 20-minute setup with permanent benefit.

---

## Infrastructure note — `__manus__` artefact in `dist/public/`

`dist/public/__manus__/` (containing `debug-collector.js` and `version.json`) is committed to the repo and served as static files. This is a debug tool injected by the Manus AI platform. It is **not** an HTML page and will not be indexed by Google, but it is an extra stray directory in the published build. Consider removing it from both `client/public/__manus__/` and `dist/public/__manus__/` in a future cleanup commit.
